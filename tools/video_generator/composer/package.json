{
  "name": "ultimatedesign-video-composer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx hyperframes preview",
    "check": "npx hyperframes lint && npx hyperframes validate && npx hyperframes inspect",
    "render": "npx hyperframes render",
    "render-low": "npx hyperframes render --quality draft -o ../output/preview.mp4"
  }
}
