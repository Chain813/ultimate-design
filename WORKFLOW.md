# 🗺️ UltimateDESIGN 城市更新平台全流程工作流 (Urban Renewal Platform Workflow)

本平台的核心使命是**面向图纸与报告生成的全流程自动化、数字化设计**。
项目最终交付的成果是一套**高精度的专业规划图册**与**项目数据可视化大屏/汇报演示文稿**。为了保障全流程的稳定运行与零运行时错误，系统将复杂的制图工作流与大模型博弈、GIS诊断引擎高度集成。

> [!NOTE]
> 📹 **关于视频模块**：视频与动画录制生成相关的模块（`tools/video_generator/`）属于平台研发的**附加拓展功能**，日常制图与报告编译核心工作流并不强制依赖此部分。

以下为基于平台 **14 个 Streamlit 页面页面**的完整标准操作指南及流转逻辑。

---

## 🛠️ 14 页面标准操作与功能指南

### 1. 📂 00_数据准备与任务解读.py (Stage 00 & 01)
- **页面目的**：上传地块空间数据，提取规划任务书刚性需求，建立物理与文本数据资产基底。
- **核心操作**：
  1. 进入 **【数据上传中心】** 上传边界红线、路网矢量文件等。统一存放于 `data/gis/`。
  2. 导入统计数据与街景样本，上传任务书 PDF/Word。
  3. 启动 **【任务解读与语义萃取】**，自动识别项目名称、设计深度和刚性指标，注入全局设计上下文（`DesignContext`）。
- **制图与成果支撑**：
  - 上传的边界红线（`Boundary_Scope.geojson`）与建筑轮廓将作为后续 ControlNet 绘图的 Canny 骨架锁定层。
  - 自动填充 A3 图册图框中的工程标题与图签基本信息。

---

### 2. 📖 02_资料收集与现场调研.py (Stage 02 & 03)
- **页面目的**：接入历史、政策等非空间规章信息，并整合现场街景多向视角，构建数字环境档案。
- **核心操作**：
  1. 确认已接入建筑、路网、用地的 GIS 物理底座。
  2. 导入现场采集的街景/全景照片切片，调用模型对街道绿视率（GVI）、界面围合感进行基线测定。
- **制图与成果支撑**：
  - 沉淀《区位分析图》与《上位规划解读图》的必备文本素材。
  - 识别出的“历史保护建筑”（紫线范围）将作为像素级 Overlay 遮罩图层，确保 AI 局部重绘时历史风貌绝对不可篡改。

---

### 3. 🔍 04_现状分析与问题诊断.py (Stage 04 & 05)
- **页面目的**：对 POI、交通和街景数据进行多维空间叠加分析，定位街区短板与高更新潜力（MPI）地块。
- **核心操作**：
  1. 开启 **【3D 现状全息底座】**，自由调整视口并叠加社会活力热力图与噪声扩散带。
  2. 进入 **【资产综合评估】**，调节 AHP 权重滑块，一键运行诊断算法，输出 `Plot_Diagnostics_Report.csv` 并生成多维分析雷达图。
- **制图与成果支撑**：
  - 直接支撑生成《用地现状分析图》、《四大问题诊断总图》、《更新潜力评价图》。
  - 高 MPI 得分地块将被作为“重点地段深化”的主舞台。

---

### 4. 🎯 06_目标定位.py (Stage 06)
- **页面目的**：结合上位政策与现状诊断结论，确定街区更新定位与设计愿景。
- **核心操作**：
  1. 系统自动载入前序诊断报告，由 LLM 精炼出四大核心设计策略（如 TOD 站城融合、文化漫步等）。
- **制图与成果支撑**：
  - 生成的设计愿景将直接融入 AI 生图的 Prompt 提示词中，从根源上控制 AI 生成画面的色彩与空间氛围协调。

---

### 5. 👥 07_设计策略.py (Stage 07)
- **页面目的**：模拟多利益相关主体（政府、开发商、居民、规划师）的博弈，寻求更新方案的动态共识。
- **核心操作**：
  1. 配置博弈角色参数与共识阈值，启动 **【多主体智能体博弈沙盘】**。
  2. 智能体自动展开多轮辩论，进行 RAG 政策合规审计，收敛生成《设计纲要》与最终《设计策略矩阵》。
- **制图与成果支撑**：
  - 协商得出的容积率、留改拆红线比例将写入空间数据库，决定后续《更新模式分区图》的各色块边界。

---

### 6. 🌇 08_总体城市设计.py (Stage 08)
- **页面目的**：确定全区宏观空间结构，实现用地规划结构优化。
- **核心操作**：
  1. LLM 自动将设计策略翻译为空间规划结构（如“一核两轴多片”）。
  2. 使用用地占比调整滑块，实时计算开发量并评估生态、交通指标扰动。
- **制图与成果支撑**：
  - 导出全局空间结构数据，为渲染《空间结构规划图》与《用地规划图》打下底板。

---

### 7. 🚦 09_专项系统设计.py (Stage 09)
- **页面目的**：针对交通、绿地、公共空间和文化路径等四大系统进行深化控制。
- **核心操作**：
  1. 调用空间分析引擎自动测算 15 分钟生活圈设施缺口、慢行绿道网络节点等。
  2. 自动生成各专项控制导语与图例样式代码。
- **制图与成果支撑**：
  - 产出《道路交通系统规划图》、《慢行系统规划图》、《公共空间网络图》、《绿地景观系统图》、《历史文化展示系统图》。

---

### 8. 📍 10_重点地段深化.py (Stage 10)
- **页面目的**：对当前重点更新单元清单中的 N 个核心潜力地块进行微观空间形态及控规指标推演。
- **核心操作**：
  1. 从 `Key_Plots_District.json` / GeoJSON 动态读取重点地块目录，查看每个地块的微观人群画像与设施缺口。
  2. 执行控规指标反推（容积率、高度、退让），由 LLM 输出具体的深化设计文字方案。
- **制图与成果支撑**：
  - 为《重点更新单元深化设计图册》提供动态地块目录、硬性技术指标与文字批注；地块数量由当前空间数据记录自动决定。

---

### 9. 🛣️ 11_实施路径.py (Stage 11)
- **页面目的**：为城市微更新方案制定合理的实施时序。
- **核心操作**：
  1. 设置建设条件滑块，一键运行生成“近期-中期-远期”实施时序表与“拆-改-留”分区方案。
- **制图与成果支撑**：
  - 输出《实施分期图》和《更新改造模式图》，并自动生成对应的分期说明文本。

---

### 10. 📝 12_城市设计导则.py (Stage 12)
- **页面目的**：将图纸与方案策略翻译为可落实的参数化控制条文。
- **核心操作**：
  1. 系统自动归并前序所有成果数据，分章节生成《城市设计导则》控制条文。
- **制图与成果支撑**：
  - 导则中的高度控制、立面材质、建筑色彩规范将直接应用于《建筑更新控制图》和《建筑高度控制图》。

---

### 11. 🎨 13_成果表达.py (Stage 13)
- **页面目的**：一键式生成高精度矢量底图并完成 A3 规划图册及 Word/docx 答辩说明书的封装。
- **核心操作**：
  1. **【规划图纸代码生成】**：一键运行 Python 空间绘图引擎，从 GIS 数据库直出高分辨率底图。
  2. **【图册自动组装】**：选择图纸编号与 A3 版式，上传渲染完成的规划图，系统自动封装标准图框、图签、图例及 AI 动态生成的规划设计说明。
  3. **【文档导出】**：导出前期诊断、总体导则及汇总报告。
  4. **【毕业设计答辩稿】**：自动调用轻量管道或全流程管道，编译输出带漂亮格式和参考文献的答辩 Word 说明书。
- **制图与成果支撑**：
  - 内置 `map_legend_right`、`dual_compare`、`analysis_dashboard`、`matrix_storyboard`、`full_bleed_effect`、`chapter_cover` 六类版式，提示词、图框封装与最终输出保持同一版式约束。

---

### 12. 📊 14_数据大屏.py (Stage 14)
- **页面目的**：为项目展示、领导汇报提供 sc-datav 风格的 Cyberpunk 浅色主题全屏数字孪生可视化看板。
- **核心操作**：
  1. 页面载入时自动通过后台获取地块内的 POI 数据、建筑高度分布及绿地占比。
  2. 自动拉伸嵌入 3D 数字孪生底座，并支持在移动端与桌面端自适应平铺展示。

---

### 13. 🎭 15_AIGC设计推演.py (Stage 15)
- **页面目的**：利用 Stable Diffusion 及 ControlNet 控制网，进行地块 Before/After 的意向风貌精细化重绘。
- **核心操作**：
  1. 选择制图任务（如建筑风貌更新），加载空间底图线稿约束。
  2. 配置采样步数、模型参数，一键渲染出竞赛级的意向设计效果图。

---

### 14. 📜 16_制图与设计智能体Skill手册.py (Stage 16)
- **页面目的**：集成大模型绘图元指令与技能规范，实现智能体的统一控制。
- **核心操作**：
  1. 挂载本地智能体技能规范（`.gemini/skills/` 目录）。
  2. 提供元指令测试与技能验证，导出规范化的制图 Skill 汇编。

---

## 📹 [附加功能] 视频生成与动画制作 (Optional Video Generator)

除 Streamlit 页面交互外，本平台在 [**`tools/video_generator/`**](file:///e:/AI-based-project/urban-platform/tools/video_generator) 中提供了独立的**大屏幕汇报视频与推演动画生成引擎**（作为系统拓展的可选参考功能）：

1. **自动脚本导演分镜**：
   - 运行视频生成脚本，引擎将通过 `stage_data_bus` 提取诊断痛点、博弈数据与指标，自动编译出一份带操作流程、镜头切换时间与数据解说旁白的 Markdown 导演分镜脚本。
2. **三维基底动画渲染**：
   - 利用 `tools/video_generator/generate_video.py`，调用底层的 Pydeck/Matplotlib/PIL 管道，将地块 3D 建筑高度生长、噪声扩散带演变及留改拆空间图层生成平滑的逐帧帧图，并一键合并导出为高清 MP4 汇报动画。

---

## 🛡️ 架构完整性与 CI 检测规范
- **路径分层规范**：所有的空间矢量数据集中在 `data/gis/`，表格型指标数据集中在 `data/csv/`，并在 `src/config/paths.py` 中实现了全局路径注册。
- **本地 CI 检测（自动化）**：每次提交代码前，需在根目录下执行 `pytest` 跑通自动化测试套件，并同步执行 `ruff check .`、`python tools/secret_scan.py`、`python tools/check_env.py`、`python tools/startup_smoke.py` 与 `python tools/data_quality_check.py`，保障没有 API 变动带来的 Regression。
- **环境变量隔离**：敏感凭证严禁硬编码。在本地环境使用 `.env` 配置文件注入 API 密钥，该文件已配置为不可提交。
