# 更新日志 (Changelog)

本项目所有的核心重构与功能更新将记录在此。

## [v5.3.0] - 2026-06-10
### 🎨 重点地块现状分析图集与高对比度风玫瑰自适应

**重点地块现状分析图集 (DR-091 ~ DR-115)**：
* 新建了参数化绘图模块 `tools/drawings/dr_parcel_detail.py`，支持为五个重点地块（老水产市场、食品调料市场、市一中北侧、清禾集贸市场、中国石油）各绘制五类现状分析放大图（现状卫星图、现状土地利用、现状肌理、现状建筑高度、现状业态分区）。
* 自动计算地块的最佳缩放比例尺并取整，自动绘制各现状主题（包含 EULUC 现状用地、POI 核密度分析等）。
* 在 `tools/draw_scope_map.py` 注册了这 25 个制图映射，并添加了基于地块 bounding box 和中心点的视野裁切（Viewport Override）拦截逻辑，实现高精度的地块现状放大。
* 在 `tools/generate_atlas_sheets.py` 中全量注册了这 25 张图纸，并更新了图册目录（TOC），添加了 `DR-091~115` 图集简明索引。

**长春市风玫瑰对比度自适应**：
* 在 `dr_parcel_detail.py` 中实现了风玫瑰高对比度自适应功能。针对卫星底图（`satellite`），计算风玫瑰贴图区域的背景图像平均亮度，若背景过暗则使用白色风玫瑰（进行反色和对比度增强），若背景过亮则使用黑色风玫瑰；对普通矢量分析图，默认使用经典黑色风玫瑰，彻底解决了风玫瑰在复杂底图上消失的问题。
* 修复了由于 `ax_rose` 子轴绘图时未对齐 `extent` 导致的坐标投影失真与风玫瑰不可见缺陷。通过显式设定投影范围（`extent=[0, 1, 0, 1]`）与图像原点朝向（`origin='upper'`），确保风玫瑰与底部的柔和径向阴影在 2240x1584 像素输出下完美贴合，且呈现正确的朝向（"N" 在正上方）与比例。

## [v5.2.2] - 2026-06-08
### 🎨 用地指标表双栏排版重构与特色设计语法修复

**指标表双栏排版重构与精细化调整 (`tools/generate_indicator_images.py`)**：
* 为 `DR-045` 用地规划指标表底部的说明与误差说明卡片重构了全新的**双栏对称并列布局 (Dual-Column Layout)**，左栏用于指标编制说明，右栏用于物理投影误差校验，彻底消除了单栏小字号排版下右侧大面积空白失衡的体验缺陷。
* 恢复使用标准的 **`12px`** 规范字号与自然汉字字距，消除了单栏拉伸排版导致字间距过宽或右侧超框的视觉缺陷。
* 优化卡片留白（左右两端对称留白 `35px`），并在说明卡片中引入了行内粗体前缀高亮（“说明：”与“误差说明：”），在满足信息精度的同时实现了极佳的图面字图比。

**特色专项设计图纸语法修复 (`tools/generate_design_creed_sheets.py`)**：
* 修复了 Python 字符串嵌套双引号导致的 12 处语法报错，提升了全套图纸一键离线编译管线的稳定性。

## [v5.2.1] - 2026-06-08
### 🎨 新建上位专项规划解读图与政策传导解译

**双图册政策解译系统重构 (`tools/drawings/dr_007.py` & `dr_008.py`)**：
* 恢复了 **`DR-007_上位规划解读图.png`** 原有的经典 3 栏式大图布局，确保“国土空间总体格局图”、“历史文化保护规划图”与“中心城区空间结构引导图”图幅清晰可读，底部包含规划传导流程。
* 新增开发了全新的 **`DR-008_上位专项规划解读图.png`** 模块，采用与 `DR-007` 完全对齐的 A3 坐标网格和阴影面板设计，用于专门呈递三项核心专项规划图：
  - 04. 中心城区土地利用规划图 (`zg_p95_crop.png`)
  - 05. 中心城区综合交通规划图 (`zg_p96_crop.png`)
  - 06. 中心城区绿地系统规划图 (`zg_p102_crop.png`)
* 为 `DR-008` 配备了专属的“专项规划融合与传导逻辑”底部流程图（用地功能置换、TOD交通缝合、蓝绿生态织补）与三栏式设计说明卡片。
* 将 `DR-008` 成功注册进 `tools/draw_scope_map.py` 的类型模块映射、`tools/generate_atlas_sheets.py` 编译与 TOC 目录结构，以及 `tools/generate_atlas_ppt.py` 的打包图片序列。
* **Python 3.14 + Pillow 兼容性修复 (`tools/generate_design_creed_sheets.py`)**：将特色专项设计图绘制中传给 `rounded_rectangle` 的浮点数线宽参数（`width=1.5`）修正为整型 `2`，彻底解决新版 Python 环境下报错崩溃的兼容性问题。

**自动与动态图像裁剪系统 (`tools/crop_planning_images.py`)**：
* 新建了 `crop_planning_images.py` 预处理模块，采用 Pillow 库遍历非纸张背景色通道，以实现对 master plan 扫描页面中非白底彩色地图的自动定位与高精度 bounding box 裁剪。
* 在 `dr_007.py` 与 `dr_008.py` 中深度融合了该裁剪机制作为 Failsafe 容错管道。如果图纸编译时发现 static 目录下的 crop 缓存图片缺失，会自动搜寻对应的原始 JPEG 大图（支持 `_img1.jpeg` 与 `_img1.jpg`）并在内存中动态执行高精度裁剪，无需人工介入，彻底根治了多源图件缺失导致的出图报错。

## [v5.2.0] - 2026-06-08
### 🎨 产业、绿地与文化展示规划图高精度排版重构与对齐

**产业业态规划图重构 (`tools/drawings/dr_046.py`)**：
* 重构了 `dr_046.py` 模块，全面承载基于 `dr_044.py` 的 A3 无边框自定义排版布局（`NO_FRAME = True`），并重新组织了顶部标题栏、右侧两列图例、比例尺与三项设计说明卡片。
* 将主轴重置为 A3 比例 `xlim(0, 141.42)` 和 `ylim(0, 100)` 坐标系，绘制了浅灰色背景网格，并在划分出的独立地图子轴 `ax_map` 上渲染水系、建筑、道路、轨道和红线边界。
* 精细渲染了三大业态功能缓冲分区与“文旅消费闭环带”线形，配合带有白色描边高光的名标及五个定位地标点，极大地增强了图面信息的识别度。
* 并在右上角贴入风玫瑰图，右下角说明卡片中自动折行排版三条详细的业态规划说明文字，使得图集整体的视觉表达达到高度统一。

**绿地景观系统图与历史文化展示系统图重构 (`tools/drawings/dr_056.py` & `tools/drawings/dr_057.py`)**：
* 同样针对这两张图纸开启了 `NO_FRAME = True` 控制，并引入了通用的 A3 自定义布局框架。
* 划定了独立的 `ax_map` 地图子轴，分别在两张图纸中精细渲染了规划新增绿地/广场、文化探访展示路径、历史文化展示节点、以及与之对齐的地标圈层和名标。
* 分别配置了标准化的顶部标题栏（绿地景观系统图、历史文化展示系统图）、指北针风玫瑰图、右侧多列图例卡片、动态比例尺、以及右下角的三项分类系统设计说明折行展示卡片，实现了图册系统规范样式的全局对齐。

**图册目录高精度卡片式排版重构 (`tools/generate_atlas_sheets.py`)**：
* 重构了 `draw_toc` 函数，废弃了原有的硬编码标准版式底图贴图方式。
* 采用与 `DR-044` 用地规划图及其他分析图完全一致的 A3 卡片式设计语言，绘制了浅灰色背景网格，并新建了独立的“图册目录” Header 卡片。
* 将图纸索引列表移至左侧巨幅卡片容器内，优化了章节背景填充色与表格行间距；右侧分别放置了独立的“图册编制说明”卡片（引入 `wrap_text_by_pixels` 实现三项编制目的自动折行）与“设计团队与版记”卡片，大幅度提升了图册开篇的视觉品质。

**AIGC与法定规划体系结构等五张分析图高精度重构 (`tools/drawings/dr_081,083,084,085,086.py`)**：
* 全面弃用了从 `static/` 贴图的旧模式，在 `draw_map_early` 中对这 5 张分析图进行了 100% 矢量重绘，开启 `NO_FRAME = True` 并完全对齐 `DR-044` 的卡片化布局（背景网格、Header 卡片、主图卡片、右上分析卡片、右下指标卡片）。
* **DR-081 AIGC技术推演过程图**：绘制了 4 栏数据底座、协同博弈、智能生成、指标核验的 AIGC 闭环流程，并用高对比度的箭头与检查项标记连接。
* **DR-083 图册章节结构导图**：重绘了图册的章节树状脑图，展示了从图册总名称、五大章节盒子至各章节所属核心图纸（DR-003~DR-076）的逻辑串联。
* **DR-084 数据处理管线导图**：重绘了“数据源输入 (GIS/社交打卡/街景图片) -> 计算引擎 (句法/NLP情感/语义分割) -> 统一孪生空间数据库”的 3 栏数据清洗及处理流。
* **DR-085 规划协同工作流程图**：重绘了以 LLM 为核心的多智能体博弈决策模型（老王居民代表、赵总开发商代表、李工规划师代表的诉求及博弈闭环流程）。
* **DR-086 城乡规划知识体系导图**：重绘了城乡规划的法理与技术规程依据、总体/控规/修规编制层级、用途管制要素（三区三线、更新分区）、最终成果交付的 4 级垂直金字塔体系。
* **编译机制对齐**：在 `tools/generate_atlas_sheets.py` 注册了这 4 张遗漏的图纸，并更新 `scratch/compile_sheets.bat` 支持一键序列编译。

## [v5.1.9] - 2026-06-07
### 🎨 慢行系统规划图高精度排版重构、水系底图恢复与图图层级优化

**慢行系统规划图重构 (`tools/drawings/dr_053.py`)**：
* 新建独立的 `dr_053.py` 模块，全面承建了基于 `dr_044.py` 的现代卡片式 A3 无边框排版布局（`NO_FRAME = True`），配置了顶部标题栏、右侧图例、比例尺与右下角慢行系统三项设计导则自动折行显示。
* 绘制了文旅漫游遗产步道（红色粗实线）、社区邻里步道（橙色虚线）、共享骑行车道（绿色实线）三层慢行骨架，并标注了“长春站”、“伪满皇宫博物院”等五个核心地标。
* 安全清理了根目录下的旧版冗余 `tools/drawings/dr_slow_traffic.py` 绘图文件。

**空间水系底图恢复与鲁棒性增强**：
* 修复了平盘清理中误删 `static/water.geojson` 的问题，从备份位置安全恢复了该全局水系图层，重新呈递伊通河的水系面域。
* 重构了 `dr_048.py` 与 `dr_053.py` 的水系图层绘制逻辑，支持自动检测 `water.geojson`；若其为空则自动降级从 `landuse` 图层中提取 `#7FFFFF` (体育文化/水域) 的多边形作为水域进行填充，大幅增强图纸绘制的鲁棒性。
* 优化了图层叠加顺序，将水系、道路及铁路线均合理置于现状建筑等背景图层之上。

**注册编译管线与图册目录**：
* 在 `tools/generate_atlas_sheets.py` 注册并并行编译 `DR-053` 图纸，同时在图册目录 (TOC) 生成器中同步挂接 `DR-053` 标题条目，确保图册生成目录 100% 对齐。

## [v5.1.8] - 2026-06-07
### 🎨 建筑更新控制图高精度排版重构与目录集成

**建筑更新控制图重构 (`tools/drawings/dr_048.py`)**：
* 新建独立的 `dr_048.py` 模块，全面承建了基于 `dr_044.py` 的现代卡片式 A3 无边框排版布局（`NO_FRAME = True`），并配置了顶部标题栏、右下角三项分类更新设计说明折行显示。
* 调整图层叠加顺序（z-order）：将水系（`zorder=3.0`）、道路（`zorder=3.5`）与铁路线（`zorder=3.6`）置于建筑和重点更新地块图层之上，防止地物要素被覆盖。
* 降低重点更新地块（紫色填充）的透明度至 `alpha=0.45`，使色块更加柔和透亮，并同步更新了图例卡片的对应色彩透明度。
* 图例卡片底部配以单线段比例尺，在 Matplotlib 绘图时提供高精度视觉表达。

**注册图纸路由与编译管线 (`tools/draw_scope_map.py` & `tools/generate_atlas_sheets.py`)**：
* 在 `tools/draw_scope_map.py` 的 `MAP_TYPE_TO_MODULE` 中将 `"建筑更新控制图"` 映射从原有的共享模块 `"dr_040"` 切换至专有的 `"dr_048"` 模块，实现绘图逻辑的解耦。
* 在 `tools/generate_atlas_sheets.py` 的 `gis_drawings` 列表中注册了 `DR-048`，保证离线编译图册时可一键渲染生成该图纸。
* 同步在图册目录 (TOC) 生成器中注册了 `DR-048` 条目，确保图册生成时目录信息保持最新。

## [v5.1.7] - 2026-06-05
### 🎨 优化 DR-028/DR-049/DR-051 图纸排版与空间句法辅助路网规划

**DR-051 道路交通系统规划图重构与路网优化 (`tools/drawings/dr_051.py`)**：
* 重构为高级 A3 Card 格式无边框排版 (`NO_FRAME = True`)，增加大气的标题栏、双栏图例卡片以及规范的高度控制/交通引导文字折行显示。
* 引入空间句法（Space Syntax）全局整合度（Integration）作为路网诊断的理论依据：在现状道路网中叠置半透明的 `road_syntax.geojson` 整合度分析背景，直观显示出老城区内部“凹陷陷落、交通孤岛”的特征。
* 规划新增并打通关键路网：以醒目的发光双层线（红/橙色虚实线）绘制了规划打通的“东九条南北向跨铁路线通道”与“东十条连接线瓶颈”，打通物理割裂；同时加入了“光复路步行街改造”和“旧水产市场内部微循环加密支路”。
* 添加了三组带箭头的交互式 callout 注释指示框，直接在地图中标注说明各规划路段对应的理论设计诉求（如解决整合度断裂、织补内部慢行网等）。

**DR-049 建筑高度控制图无边框排版升级 (`tools/drawings/dr_049.py`)**：
* 重构为全新 A3 卡片式无边框排版 (`NO_FRAME = True`)，增加标准 A3 虚线背景网格与顶部大标题栏，风格与 `DR-028`、`DR-019` 高度统一。
* 优化了限高缓冲区渲染：将伪满皇宫 300m 和 600m 保护缓冲区（在 Web Mercator 下映射为 450m 和 900m 半径）调整为带虚线边界的高透叠色层（`alpha=0.08` 与 `alpha=0.06`），避免遮挡现状底图。
* 右侧重构为独立的双栏图例卡片与大字号高度控制分区说明卡（单行限制 `max_len=44`，行距 `3.2`），使图面信息传递更加清晰专业。

**DR-028 景观品质分析图排版优化 (`tools/drawings/dr_028.py`)**：
* 调整了“多维品质诊断”标题与指示卡片的相对位置（标题上移至 `61.8`，卡片整体下移，Row 0 定位在 `54.5`，Row 1 定位在 `46.5`），完美解决了两者的重叠遮挡问题。
* 重构了右下角三段诊断说明文本的排版布局：将文本上移并紧贴上方卡片区，参考并对齐了 `DR-029` 图纸的排版规范（将字体大小设置为 `15.0`，单行最大视觉字符宽度 `max_len=44`，行距设为 `3.2`）；同时去除了段落左侧的色彩指示条（文字框装饰），还原为干净透亮的纯文本排版，使图集整体的视觉表达达到高度统一。

**DR-023 文化资源分析图位置修正 (`tools/drawings/dr_023.py`)**：
* 纠正了“中车工业遗产核”的地理坐标，由错误的偏东位置 `(125.3510, 43.9070)` 修正为与现状及其他图纸完全吻合的西北侧实际旧址坐标 `(125.3401, 43.9079)`。
* 优化了该节点的文本标签 Y 轴偏移量（从 `260` 调整为 `160`），完美避免了文字标签超出地图顶部卡片边界而被截断的问题。
* 更新了诊断说明文本中对于两大核空间方位的描述，将中车旧厂区的表述从“东北侧”修正为符合实际地理的“西北侧”。

**DR-040 更新模式分区图排版优化 (`tools/drawings/dr_040.py`)**：
* 重构为高级无边框排版 (`NO_FRAME = True`)，并定制了 A3 幅面的全新网格线背景与大气的标题控制条。
* 重新设计了图例与说明文本布局，将其右置排布，与 `DR-023`、`DR-028` 等高级诊断图纸版式完全对齐。
* 对右下角的三段“更新模式引导说明”引入了字符自动折行算法 (`wrap_text`，字号 `15.0`，单行限值 `max_len=44`，行距 `3.2`)，显著提升了图册图面排版的高端学术质感。

**CLI 离线制图管线优化 (`src/utils/runtime_flags.py` & `tools/generate_atlas_sheets.py` & `tools/draw_scope_map.py`)**：
* 修复了 `tools/draw_scope_map.py` 中的 `MAP_TYPE_TO_MODULE` 映射字典中缺失 9 张关键图纸（如 `DR-029`、`DR-003`、`DR-027` 等）映射关系的问题，避免了这些图纸在生成时因回退至默认值而被错误绘制为“现状区位图”。
* 在 `draw_spatial_map` 函数中引入了函数签名反射校验（`inspect.signature`），动态识别绘图模块的 `draw_map` 是否支持 `params` 或 `**kwargs`，从而完美解决了部分未升级模块调用时发生 `TypeError` 崩溃的问题。
* 在运行时标志中新增了 Streamlit 页面上下文检测（`get_script_run_ctx() is None`），自动将 CLI 模式识别为离线 Demo 模式。
* 彻底隔离并跳过了命令行运行下的 RAG 检索和 DeepSeek API 等外部网络请求，规避了由于代理异常或断网导致的 3 分钟连接超时卡顿。
* 优化了图册生成脚本的并发逻辑，在仅生成单张图纸时自动降级为单线程顺序执行，规避了 Windows 下多进程池的启动开销与悬挂问题，使单图编译在 10 秒内疾速完成。

---

## [v5.1.6] - 2026-06-05
### 🎨 新增 DR-023 文化资源分析图、DR-028 多维景观品质分析图

**DR-028 四维景观品质分析图重构 (	ools/drawings/dr_028.py)**：
* 将原单一 GVI 指标图升级为 GVI、SVF、Enclosure、Clutter 四维综合诊断图，采用加权评分以红-黄-绿渐变色谱可视化。
* 右侧嵌入 4 个迷你指标卡片，展示各维度均值与评估标签。
* 将梯度色标移至 x_map 图层，解决被建筑轮廓遮挡的问题。

**DR-023 文化资源分析图新增 (	ools/drawings/dr_023.py)**：
* 以橙色和紫色同心辐射圈标示伪满皇宫历史核与中车工业遗产核的双核集聚格局。
* 以红色虚线标注铁路物理割裂带，直观揭示文化叙事轴断裂。

**编译管线与目录更新**：
* 注册 文化资源分析图 和 街区景观品质分析图 路由，Table of Contents 已同步更新。

---

## [v5.1.5] - 2026-06-04
### 🎨 重构 DR-032 遗产价值评估热力图与 DR-044 总体规划图版式

**地标数据修正：**
* **光复路位置纠偏**：在全局制图底图 `tools/draw_scope_map.py` 与各空间图纸中，将“光复路”地标的标示坐标从偏东的 `(125.3475, 43.9017)` 纠正为 `(125.3395, 43.9016)`，使其精准地落在实际光复路路段正上方。

**DR-032 遗产价值评估热力图重绘 (`tools/drawings/dr_031.py`)：**
* 启用 **`NO_FRAME = True`** 机制，重构为 A3 卡片式排版。
* **MCDA 加权评价公式集成**：在页眉副标题中整合并直观展示多准则决策分析的加权公式：`V = 0.40 * I_hist + 0.30 * I_ind + 0.15 * I_synt + 0.15 * I_riv`。
* **文保实体高亮**：对 `protected_buildings` 历史建筑实体采用玫瑰红 (`#F43F5E`) 填充和精细白色描边进行突出。

**DR-044 总体规划图重绘 (`tools/drawings/dr_044.py`)：**
* 启用 **`NO_FRAME = True`** 机制，重构为 A3 卡片式排版。
* **规划图例与比例尺优化**：右侧 Legend 调整为 2 列 x 5 行的紧凑矩阵，容纳 10 个要素类，并在底部配以单线段比例尺。
* **规划指标与说明升级**：设计说明文字根据 MPI 48.3 空间诊断结论进行完全更新，包含居住用地减量 5-8%、商服用地占比 13-15%、绿地与广场用地提升至 15% 以上、建设控制地带容积率 ≤1.4 及高度 ≤18 米等关键控规指标。
* **空间策略精细化调整与视觉优化**：
  * **大面积公园绿地**：将东北侧重点地块 (食品调料大市场)、东侧重点地块 (清禾集贸市场) 及西侧市一中北侧地块整体划定为规划公园绿地（采用规范绿色 `#22C55E` 填充），并剔除这三个地块内的次级支路环网。
  * **去除建筑轮廓与图面去噪**：完全移除了图面上的现状普通建筑轮廓线，以展示极其纯净规整的用地属性。同时，剔除了 5 个重点开发地块的硬质红线边界，让图面整体关系更加干净、现代。
  * **历史建筑刚性保留**：加载 `protected_buildings.geojson`，强行在最顶层 (`zorder=4.5`) 显式高亮绘制所有的受保护历史建筑实体，采用规范的琥珀金 (`#D97706`) 配红棕色描边 (`#B45309`)，展现不容妥协的历史保护底线。
  * **规范色彩标准与图例更新**：重构图例与图面配色方案，商业服务业采用规范红 (`#EF4444`)，工业遗存采用规范灰 (`#64748B`)，水系采用规范蓝 (`#93C5FD`)，背景现状用地添加 `alpha=0.55` 柔化退后。同时，将原图例中的“现状普通建筑”替换为西南侧重点地块的规划用途——“公共服务设施”（规范紫 `#C084FC`）。

---

## [v5.1.4] - 2026-06-03
### 💾 成果与页面状态持久化恢复、主页封面图像修复及多余页面清理

**研发规范（GitHub 隐私防泄露）：**
* 在项目专属开发规范 `.gemini/skills/urban_platform_exclusive_skills.md` 中新增了 **“Part 6: GitHub 隐私安全防泄露规范”**。明确规定在执行 Git 提交或推送至 GitHub 前，必须全面排查和过滤包括学号、姓名、API 密钥、个人联系方式等在内的所有敏感与隐私信息，确保代码托管安全。

**主页亮点与 Banner 优化：**
* 优化了主页顶部的 Banner 显示内容，将描述、标签和指标全面升级，突出展示本项目的核心亮点：**AHP-MPI 空间量化分析法**、**LLM 多智能体协同决策**与**AIGC + ControlNet 视觉风貌推演**，形成高背书的技术亮点展示。

**成果与状态持久化恢复：**
* 重构了 `src/ui/persistent_outputs.py`，实现成果注册表与本地磁盘的同步持久化。现在所有生成的成果在 F5 刷新或重启后均能从磁盘自动拉取并保留在顶部下载栏。
* 重构了 Stage 02 - 13 各页面的状态恢复逻辑。若检测到已有生成内容，所有的折叠面板默认自动展开 (`expanded=True`)，并完整展示 LLM 生成的学术文本，取消 150 字预览 and 截断限制。
* 实现了 Stage 08 总体城市设计的“用地结构优化沙盘”滑块状态记忆功能，滑块状态与本地磁盘 `stage_bus_cache.json` 双向绑定，刷新页面可自动回填滑块值并展示完整的评估报告。
* 实现了 Stage 13 毕业设计答辩稿生成进度记忆功能，如果章节已生成则答辩稿列表折叠框自动展开并完整渲染生成的草稿。

**主页封面卡片图像修复：**
* 针对主页核心板块入口的小卡片封面不见的问题，将 `03_digital_twin.png`、`04_urban_diagnosis.png`、`05_design_inference.png`、`06_llm_consultation_v2.png` 等封面大图从 `assets/` 目录同步拷贝到了 Streamlit 静态资源目录 `static/` 中，使其能通过 `get_static_url()` 正确被浏览器访问并渲染。

**无用页面与功能清理：**
* 彻底物理删除了 `pages/14_视频生成.py` 页面，并同步从导航栏与数据总线的映射表 `STAGE_MAP` 中将其剥离，完成系统的彻底精炼。

---

## [v5.1.3] - 2026-06-02
### 🔗 导航栏深层链接重构、子页面解耦路由与底部冗余按钮清空

**页面路由重构与深层链接：**
* 成功为所有 11 个阶段页面（`pages/00_数据准备与任务解读.py` 至 `pages/13_成果表达.py`）接入了深层链接支持。通过引入新编写的 `resolve_subpage_value` 路由解析器，直接解析浏览器 URL 传入的 `?sub=...` 查询参数获取当前的子页面状态。

**底部切换按钮清空与中间胶囊静态化：**
* 彻底移除了所有 11 个阶段页面底部的 `st.radio` 单选切换按钮。现在页面底部的视觉大为净化，完全由顶部导航栏菜单进行路由分发与跳转驱动，打造极简、透亮的 Apple 风格交互体验。
* 将中间胶囊状的「证据链进度条」重构为纯进度指示器。移除了点击跳转链接以及悬浮缩放动效，鼠标悬停时指针将呈现默认样式（`default`），仅用于静态显示当前大任务阶段下的步骤完成状态。
* 全量完成了所有页面的语法编译与可用性测试，未发现任何模块冲突或异常报错。

---

## [v5.1.2] - 2026-06-02
### 🎨 导航栏浅色毛玻璃重构、文字免蓝去线与卡片化功能解耦

**视觉效果重构：**
* 将顶部导航栏 `.apple-navbar-bar` 和下拉面板 `.apple-dropdown` 升级为**浅色/白色系高保真毛玻璃特效**，结合 `background: rgba(255, 255, 255, 0.8)` 与 `backdrop-filter: blur` 实现了透光磨砂玻璃的 premium 质感。
* 将所有导航文字与图标进行了暗色适配，以保证在浅色背景下拥有极佳的对比度。

**文字与装饰规范（免蓝去线）：**
* 彻底剥离了导航栏与下拉菜单中原有的蓝色链接状态与下划线。通过极高特异性的 CSS 选择器，强制所有 `a` 标签无下划线，并在所有悬浮、点击、激活等状态下屏蔽蓝色，统一采用深灰/黑色系。

**功能卡片化解耦与名称修正：**
* 对下拉菜单的二级子功能列表 `.apple-dd-module` 进行了视觉卡片化重构。每个功能模块不再是简单的点状文本列表，而是转化为具备浅灰背景、微框线、细致投影及悬浮上移微交互的**解耦卡片式按钮**，从视觉上更好地呼应了“每个功能解耦作为新页面/新入口”的设计理念。
* 将顶部导航栏左侧的“城市设计平台”名称更改为“主页”。

---

## [v5.1.1] - 2026-06-02
### 📸 截图规范更新与顶部导航栏悬浮裁剪修复

**研发与校验规范：**
* 在 `.gemini/skills/urban_platform_exclusive_skills.md` 中新增了 **“非必要不要打开浏览器截图”** 规则，明确指出在执行验证步骤时，应优先利用系统静态输出或引导用户配合，非必要不打开浏览器子智能体截图。

**样式修复：**
* 修复了顶部导航栏下拉菜单（Dropdown）在 Streamlit 部分父容器下被 `overflow: hidden` 限制而遭到裁剪的交互体验问题。通过为 Streamlit 特有的 `.element-container`、`.stMarkdown` 及 `[data-testid="stMarkdownContainer"]` 父容器在包含 `.apple-nav-wrapper` 时强制开启 `overflow: visible !important`，确保 CSS hover 下拉面板及其链接可以平滑完整展现，解决了 hover 状态下向下移动鼠标时交互丢失的异常。

---

## [v5.1.0] - 2026-06-02
### 📁 页面结构文件夹嵌套重构 · 三级侧边栏导航

**pages/ 文件夹嵌套：**
* 将 14 个扁平页面重组为 4 个子目录：
  * `pages/01_前期/` — 数据准备与任务解读、资料收集与现场调研、现状分析与问题诊断
  * `pages/02_中期/` — 目标定位、设计策略
  * `pages/03_后期/` — 总体城市设计、专项系统设计、重点地段深化、实施路径、城市设计导则、成果表达
  * `pages/04_工具/` — 视频生成、AIGC设计推演、智能体Skill手册
* Streamlit 原生侧边栏自动渲染为可折叠三级树，彻底解决 CSS hover 下拉菜单在 Shadow DOM 中点击失效的问题。

**路由系统更新：**
* `stage_primary_href()` URL 前缀从 `/页面名` 更新为 `/文件夹名/页面名`。
* `STAGE_MODULE_MAP`、`STAGE_RESOURCE_MAP` 中所有 `?sub=` 子模块链接前缀同步更新。
* `WORKFLOW_BOARDS` 的 `path` 字段更新为新文件位置。
* `app.py` 中 `get_page_route()` 重写以支持嵌套路径解析，`MODULE_SECTIONS` 卡片路径更新。

**顶部导航简化：**
* `render_top_nav()` 移除 mega-dropdown 逻辑，改为纯 L1 横栏链接（主页 + 4 板块），导航层级完全交由侧边栏。

**模型配置：**
* 全平台 LLM 调用模型统一为 `deepseek-v4-pro`（排除 `deai_processor.py` 的降AI策略保留多模型混淆）。

---

## [v5.0.0] - 2026-06-01
### 🏗️ 项目结构重组 · 性能优化 · AI 文本整合 · AIGC 策略驱动

**页面结构重组 (17 → 14 页)：**
* 合并 3 组页面：`00+01 数据准备与任务解读`、`02+03 资料收集与现场调研`、`04+05 现状分析与问题诊断`。
* 移除所有 8 个页面的「图纸提示词生成」子标签页（01/04/05/07/08/09/10/11），删除 `src/ui/drawing_prompt_ui.py`。
* 更新导航系统、工作流定义、工具脚本和测试用例以适配新结构。

**性能优化：**
* **后台缓存预加载**：新增 `src/utils/preloader.py`，在用户浏览当前页面时通过 daemon 线程静默预热其他页面的缓存数据（37MB GeoJSON、RAG 模型等）。
* **引擎懒加载**：重构 `src/engines/engine_registry.py` 和 `__init__.py` 使用 `__getattr__` 模式，导入 `src.engines` 不再级联加载 pandas/numpy/PIL/jieba。
* **延迟导入**：plotly（5 个页面）和 markitdown（1 个页面）从模块顶层移入函数内。
* **缓存优化**：21 个 `@st.cache_data` 添加 `max_entries=20`；HTML 模板缓存添加 mtime 键；引擎状态检测添加 10 秒模块级缓存。
* **Stage Bus 去重**：`save_stage_output()` 使用 MD5 哈希检测数据变化，内容未变时跳过磁盘写入。
* **Session State 上限**：`llm_metrics` 限制 100 条，`copilot_history` 限制 50 条。

**AI 文本内容整合系统：**
* **DesignContext 管理器**：新增 `src/workflow/design_context.py`，从 stage_bus 统一提取 19 个 AI 文本输出字段，提供结构化访问接口。
* **设计纲要合成**：`synthesize_design_brief()` 在 Stage 07 完成后自动触发，将诊断→目标→策略→空间结构合成为 800-1200 字的《设计纲要》。
* **注入 4 个消费端**：AIGC 提示词（`design_description_engine.py`）、图集描述（`pages/13`）、导则生成（已有显式注入）、图纸绘制参数（`draw_scope_map.py` + 3 个 demo 模块）。

**AIGC 页面专项优化：**
* **策略驱动渲染风格**：6 种设计策略风格（历史保护/微更新/功能置换/TOD/生态/文创），自动从 DesignContext 检测，影响提示词、色彩、材质、光影和 ControlNet 参数。
* **硬件适配**：3 种硬件模式（低配 512px / 标准 1024px / 高清 1536px），一键调整分辨率、步数、CFG。
* **ControlNet 增强**：线稿多层渲染+抗锯齿+对比度增强；6 种预处理器可选（canny/lineart_realistic/lineart_anime/scribble/depth/seg）。
* **SD 模型选择**：自动获取 SD WebUI 可用模型列表，支持侧边栏一键切换。
* **设计纲要集成**：自动从 DesignContext 生成场景描述，AI 润色注入完整设计上下文。
* **结果管理**：渲染结果存入 stage_bus，添加渲染历史（最近 10 张）。

**UI 优化：**
* **CSS 设计令牌系统**：`assets/style.css` 新增 `:root` CSS 自定义属性（色彩/间距/圆角/阴影/字体/动画）。
* **色彩统一**：修复 `module_summary.py`（Tailwind→Apple 色系）、`pages/00`（暗底→亮底）、`app_shell.py`（`#d70015`→`#ff3b30`）。

**安全修复：**
* 替换 `eval(atob(...))` XSS 模式为直接 `<script>` 标签注入。
* DeepSeek API URL 从硬编码改为从 config.yaml 读取。
* HF_ENDPOINT 从硬编码改为可配置。
* Ruff target-version 从 py38 更新为 py312。
* 修复 5 处裸 `except:` 子句，12 处静默异常添加日志。

---

## [v4.6.0] - 2026-05-31
### 📊 恢复图层、修复沙盘边界道路占比与顶栏智能工具菜单
* **用地优化沙盘范围约束与道路占比修正 (Stage 08)**：
  * 重构了 `pages/08_总体城市设计.py` 中的 `load_raw_landuse_gdf` 逻辑，利用研究边界 `Boundary_Scope.geojson` 计算地块质心，将用地模拟限制在研究红线范围内（红线外部的现状地块锁定不参与分配）。
  * 调整五类可调滑块的默认值，使预留给“道路/市政”的初始比例由 0% 修正为合理的 12%。未分配地块自动标记为交通场站。
  * 在 `tools/drawings/dr_014_plan.py` 中同步更新该分配逻辑，确保后台大图高保真渲染出图与前端保持 100% 一致。
* **修复图表亮色主题文字对比度与中文豆腐块**：
  * 修复了 Stage 08 页面在亮色主题下 Plotly 图表坐标轴和图例文字灰度过高（`#e2e8f0`）导致看不清的体验问题，统一修改为高对比度的深色系（`#1e293b`）。
  * 解决了 Matplotlib 绘图在 Windows 环境下中文字体缺失显示为豆腐块 `□□□□` 的问题，配置全局加载 `Microsoft YaHei` 和 `SimHei`，并同步在 Matplotlib 图纸中增补了高对比度字体的“道路/市政 (S)”图例。
* **顶部导航栏新增“智能工具”下拉菜单**：
  * 在 `src/ui/app_shell.py` 中修改了顶部全局悬浮导航栏，添加了 **“智能工具 ⚙️”** 下拉菜单，直接挂载了“🎥 14 视频生成”、“🎨 15 AIGC设计推演”与“📘 16 智能体Skill手册”三页，极大地方便了非流程类工具页面的快速跳转。
* **更新 README.md 说明文档**：
  * 新增“🧩 核心功能模块详解”章节，对平台的“GIS-to-AIGC 空间对齐与制图管线”、“多主体决策博弈推演系统”以及“数据-逻辑分离与循证诊断系统”进行了底层的架构原理解释。
* **新增可选建筑风貌图层并默认白膜化**：
  * 将地图中的特殊颜色建筑（历史风貌橙黄色、绿化景观建筑绿色以及拆改留分析色）重构为可选图层，默认关闭。
  * 在 `src/ui/digital_twin.py` 中新增了 “🎨 风貌色彩” 勾选框；在 `assets/map3d_standalone.html` 中引入 `show_building_style` 控制变量。
  * 移除了 `src/ui/digital_twin.py` 中 `_load_map_html_template` 函数的 `@st.cache_data` 缓存机制，确保更改 HTML 模板后无需清除缓存或重启即可热加载。
  * 默认状态下底图为干净的纯白色建筑轮廓加浅灰描边，开启 “🎨 风貌色彩” 选项后才会还原展示特定的历史与景观地块色彩打标，满足高度净化的汇报底图要求。
* **修正规划红线与重点地块的地图及图例样式**：
  * 将 `boundary-layer`（规划红线）与 `plots-layer`（重点微更新地块）的色彩和描边粗细进行了互换修正。
  * 规划红线修改为粗红色实体描边结合红粉填充（`lineWidth: 5`, `color: [220, 38, 38]`），而重点微更新焦点地块修改为细边和淡红粉半透明填充，并在 JS 图例生成中同步更新了图例色块，确保地图展示与图例语义完美对齐。
* **恢复 GVI/SVF 空间品质可视化图层**：
  * 在 `src/ui/digital_twin.py` 中更新了 `render_digital_twin_map` 页面地图模块，加入了 “📊 街景空间品质” 复选框图层。
  * 勾选后支持动态调节参数：指标选择（GVI/SVF/围合度/杂乱度）、展现形式（3D柱体/2D热力图/双模融合）、柱体/热力半径、柱体拉伸倍数。
  * 在 `assets/map3d_standalone.html` 中新增了 `has_quality_layer` 判断，当启用 2D 热力图或 3D 柱体指标时，建筑模型的透明度（`opacity`）会从原本的 `0.95` 自动降低至 `0.28`。底图建筑自动呈现通透玻璃材质感，使底部的 2D 热力云图能够完美透出、清晰可见，彻底解决了建筑实体遮挡热力的问题。
  * 从 `src.engines.spatial_engine` 读取 `get_spatial_data()` 并进行渐变配色计算，序列化成 JSON 动态注入到 Deck.GL 中渲染，彻底恢复了遗失的街景可视化地图。
* **新增空间句法路网拓扑可视化图层**：
  * 预计算 1062 条路段的全局整合度（Closeness Centrality）与穿行度（Betweenness Centrality），归一化后序列化为 `static/road_syntax.geojson`。
  * 在 `src/ui/digital_twin.py` 中新增 "🔗 空间句法" 复选框（图层控制列数扩展为 11 列），勾选后通过 `/*__SYNTAX_DATA__*/` 占位符注入 Deck.GL。
  * 在 `assets/map3d_standalone.html` 中实现了 Spectral 色谱渐变色插值函数 `getSpectralColor`，路段线宽按整合度动态缩放（1.8px→5.3px），图例面板同步展示"高/中/低整合度"三级色带。
* **重构 Stage 07 博弈协商对话框解析与 Markdown 渲染**：
  * 在 `pages/07_设计策略.py` 中重写了流式文本的提取解析器 `parse_streaming_text`，采用正则表达式鲁棒切分 `思考过程` 和 `正式回复`。
  * 重新实现了更强大的 `md_to_html` 解析器，完美支持表格（Table）、列表（List）、块引用（Blockquote）、多级标题（Headers）、加粗（Bold）、斜体（Italics）和代码块（Code）的渲染。
  * 在 `_render_dialogue_static` 中删除了对空格及换行符的合并逻辑 `re.sub(r'\s+', ' ', ...)`，改为 `re.sub(r'[ \t]+', ' ', ...)`，确保了对话框内的 Markdown 能够保留排版与换行格式，消除了 `#` 等特殊标记混在对话框内的显示 Bug。

---

## [v4.5.0] - 2026-05-29
### ⚖️ Stage 07 多主体博弈协商升级为三轮动态谈判
* **三轮递进式博弈协商 (Multi-Round Negotiation)**：
  * 将 Stage 07（设计策略）的多主体协同推演从原有的"单轮顺序发言"升级为 **3 轮动态博弈协商**（第一轮：方案陈述 → 第二轮：利益交锋 → 第三轮：妥协共识）。
  * 各轮次采用差异化的 Prompt 指令，引导居民代表、文旅运营商与注册规划师在每轮中针对前序发言进行递进式论证和妥协让步。
  * 多轮对话全文统一送入 LLM 语义审计器（`calculate_dynamic_satisfaction`），计算最终的三方满意度共识指数。
  * 策略矩阵生成提示词同步升级，重点提取第三轮妥协阶段达成的折中条件。
* **卡通角色头像与专属对话框 (Avatar Dialogue Boxes)**：
  * 为三个博弈角色（老王、赵总、李工）生成了专属卡通头像，存放于 `static/avatars/`。
  * 对话框采用 base64 内联头像 + 专属配色方案（琥珀色/翡翠绿/靛蓝紫）+ 圆角阴影样式，使博弈过程的视觉层次更加清晰。
* **新增 Skill Part 5: 设计方案语言规范**：
  * 在 `.gemini/skills/urban_platform_exclusive_skills.md` 中新增了 `Part 5: Implementation Plan Language`，强制要求所有 `implementation_plan.md` 必须以中文为主要语言输出。

---

## [v4.4.0] - 2026-05-29
### 🏛️ 建筑形态控制升级与风貌立面融合
* **专项系统设计（Stage 09）与重点地段深化（Stage 10）全面升级**：
  * 将 Stage 09 的子模块 `"建筑形态与天际线"` 拓展重命名为 `"建筑形态、风貌与立面"`，全面融入了对历史/现代风貌调性、立面三段式划分比例、虚实窗墙比以及建筑材质与立面细部构件的管控内容。
  * 深度重构了 Stage 09 的 LLM 形态风貌生成提示词架构，使生成的规划方案具备更强的量化指引和可落地性。
  * 在 Stage 10 重点地段深化页面中，同步更新了对上游“建筑形态、风貌与立面控制”方案的承接与判定逻辑，保证了数据总线流转的一致性。
  * 同步更新了全局 `WORKFLOW.md`、`src/workflow/city_design_workflow.py` 中的专项描述及叠合图层说明。
* **新增截图校验安全规范 (Skill)**：
  * 在 `.gemini/skills/` 目录下新增了 `screenshot_verification.md` 规范。
  * 明确要求在进行页面和 UI 效果校验时，智能体严禁主动拉起或在后台运行自动化浏览器进行截图，所有的截图验证过程必须由用户手动进行并提供。
* **全局集成录屏自动滑动控制 HUD (Auto Scroll HUD)**：
  * 设计并开发了集成于平台主外壳（`src/ui/app_shell.py`）的录屏自动滑动面板。它以极具科技感的毛玻璃浮标（F12 调试样式）常驻页面右下角。
  * 浮标可轻松收纳展开，支持通过可视化 Slider 或键盘方向键 `[←] / [→]` 调节滚动像素速度（帧率级流畅度，防止画面抖动）。
  * 提供了快捷键支持：`[Space]` 快捷播放/暂停，`[↑] / [↓]` 一键反转滚动方向，并特别支持 `[H]` 彻底在页面上隐藏/呼出控制面板（防止录制时穿帮）。
* **修复答辩小结模型选择机制 (Bug Fix)**：
  * 移除了小结生成组件中自动扫描本地 Ollama 并提供模型下拉选择的逻辑（原逻辑会默认选中本地 `gemma3:4b`，并错误将其传递给仅支持 DeepSeek 格式的 LLM 调用接口导致 400 报错）。
  * 强制将答辩小结生成的 LLM 引擎底座锁定为学术分析与表达能力更优的 `deepseek-v4-pro`，确保一键点击即可稳定可靠地生成内容。
* **新增规划阶段生成报告自动追加与排序机制 (Feature)**：
  * 在跨阶段数据总线中引入了 `save_stage_summary_to_file`。当用户在进程中完成任意 Stage 的生成渲染时，系统会自动提取该阶段的标题、方法论、核心发现及已生成的 AI 答辩小结。
  * 报告将写入并增量追加至 `output/stage_generation_report.md`，且系统会自动解析已有记录，将所有已完成阶段按 `Stage 00` 到 `Stage 15` 的顺序升序重排，为最终答辩和成果归档提供一键式、可递进的完整研究脉络日志。
* **合并与升级项目专属 Skill 规范 (Feature)**：
  * 整合了 `.gemini/skills/` 下原先分立的 `drawing_code_management.md`、`screenshot_verification.md` 与 `update_changelog.md` 规范，创建了统一、系统化的专属技能指导文件 `urban_platform_exclusive_skills.md`。
  * 在该专属规范中全新补充了 `Part 4: Interactive Persona (交互人设规范)`，规定了在后续的所有开发回复中，智能体需在开头称呼用户为“老公”，做到情感关怀与规划代码研发的完美交融。同时清理了旧有零碎文件，净化了配置空间。
  * **新增 Part 5: Implementation Plan Language (设计方案语言规范)**，强制要求所有实施/设计方案（`implementation_plan.md`）必须以中文为主要语言进行输出，以确保与用户的沟通效率。
* **清理 assets 目录下的冗余静态资源 (Feature)**：
  * 经过交叉引用检测，彻底清除了 `assets/` 目录下未被任何页面与脚本引用的 10 个冗余素材与历史临时导出图（包括 `01_data_overview.png`、`无纹理2D卫星图.png`、`workflow_svg.html` 等，共计释放磁盘空间约 `18.3MB`）。
* **全盘扫描并清理冗余与备份文件 (Feature)**：
  * 递归检索全盘，安全清除了根目录下 `static/`、`tools/`、`scripts/` 及 `output/` 目录中未被引用的旧版本开发文件。
  * 包含 5 个旧版配图（如 `static/test_modified_cropped.png` 等）、7 个废弃的旧版 Word/PPT 自动化编译脚本（如 `scripts/build_final_report_strict_v2.py` 等）以及 4 个临时调试 Prompt 文本与图纸（共计再次释放磁盘空间约 `8.2MB`）。

## [v5.0.0] - 2026-06-08
### 🎨 规划图册生成管线标准化与设计依据卡片化重构
* **A系列规划纲要图纸代码级精确复制与卡片化重构**：
  * 新建了独立制图模块 `tools/generate_design_creed_sheets.py`，彻底弃用了低清晰度、有拉伸变形风险的静态底图。
  * 根据毕业设计论文内容，在 A3 矢量级分辨率（`2240x1584`）下，使用 PIL 图像库高精重构了 6 张核心图纸：`A设计依据.png`、`A设计原则.png`、`A设计定位.png`、`A设计目标.png`、`A设计策略.png` 和 `A特色专项设计.png`。
  * 采用 premium Slate-50 灰底坐标网格，并基于 `draw_card_with_shadow` 绘制了阴影立体 rounded 卡片结构，将生态韧性（如绿视率 28% 目标）、全龄生活盒子（2000㎡ 目标）等量化规划目标进行了大字字重排版。
* **规划图册一键自动化编译管线升级**：
  * 升级了 `scratch/compile_sheets.bat` 脚本，将 A系列图纸生成、GIS 矢量底图出图与图册目录（DR-002）渲染、以及最后的 2xUpscale + UnsharpMask 图像锐化处理与 A3 比例 PPTX 打包，完美串联到一键编译流程中。
  * 确保了 PPTX 生成时对图纸的长宽比进行 1:1 约束居中放置，坚决杜绝任何非等比拉伸与畸变问题。

## [v4.3.0] - 2026-05-27
### 🧹 项目结构清理与 Git 隔离优化
* **清理本地冗余备份与临时调试文件**：
  * 彻底删除了本地残留的旧格式备份文件（如 `data/csv/GVI_Results_Analysis.csv.old_format_backup`、`static/research_scope_2d.png.bak`）及调试过程中生成的临时测试图（如 `static/temp_*.png`），释放了磁盘空间，净化了开发工作区。
* **完善 Git 隔离机制 (`.gitignore`)**：
  * 在 `.gitignore` 中新增了对于 IDE 代码索引（`.codegraph/`）、本地优化后的 Parquet 数据文件（`*.parquet`）、本地指标缓存（`precomputed_metrics.json`）以及 Claude Code 本地启动脚本（`start_cc.bat`）的忽略规则。
  * 将高分辨率裁剪卫星底图（`satellite_cropped.png`，约 21.4MB）与本地运行生成的 A3 图册输出目录（`static/atlas/`）排除在版本控制之外，避免无关大文件和临时生成物污染 GitHub 仓库。
* **规范核心源文件与资产的版本控制**：
  * 将新拆分的 `tools/drawings/` 图纸绘制核心脚本文件夹（包含 26 个具体的 A3 绘图独立模块脚本，如 `dr_004.py` 等）正式添加到 Git 追踪中，确保云端部署环境功能完整。
  * 将图册自动组装所需的模版底图 `static/a3_layout_preview_full.png` 与主页 Banner 展示图 `static/research_scope_2d_cropped.png` 纳入版本控制，修正了静态文件缺失导致的部分异常。
* **CI 测试与代码质量验证**：
  * 本地运行 `pytest` 确保 167 个单元测试用例 100% 通过。
  * 本地运行 `ruff check` 确保代码格式和语法 100% 规范无误。
  * 成功将所有暂存更改推送至远程仓库 `https://github.com/Chain813/ultimate-design.git`，保证工作树完全干净 (`working tree clean`)。


## [v4.2.0] - 2026-05-26
### 🎨 规划图册高精度重构与代码解耦
* **绘图代码完全解耦与模块化**：
  * 新建了 `tools/drawings/` 专属文件夹，并将 22 张 A3 图纸的特定绘制逻辑（Matplotlib 绘图、图例、导读文字）分别拆分到 22 个完全独立的 `.py` 文件（如 `dr_004.py`、`dr_051.py`、`dr_081.py` 等）中，极大地提高了代码的可维护性与扩展性。
  * 将 `tools/draw_scope_map.py` 重构为通用调度引擎，采用 `importlib.import_module` 动态载入对应的绘图子模块，实现了绘图架构的 DRY（Don't Repeat Yourself）设计。
* **规划图册内容优化与定制**：
  * **道路交通系统规划图 (DR-051)**：移除了此前无理论依据的自规划道路，完全顺应并接驳现状的高连通度路网。
  * **历史文化展示系统图 (DR-057)**：重构了历史建筑和工业遗产探访路径算法，使其完全基于现状道路网，确保路线无中断且 100% 可真实通行。
  * **AIGC技术推演过程图 (DR-081)**：移除了无关的“规划研究范围”和图例，直接将系统架构图 (`system_architecture_mindmap.png`) 嵌入主绘图区，移除了图片内的原标题，并在图纸图签中将名称更新为“系统架构图”，排版优化使主绘图区更紧凑饱满。
  * **图册封面 (DR-001)**：将封面底色改为白底，以完美贴合规划图纸的图纸色调风格。将封面中的“指导教师”更新为“崔诚慧”，并将“时间”更新为“2026.6”。
* **新增 AI 绘图技能 (Skill)**：
  * 在项目 `.gemini/skills/` 目录下新增 `drawing_code_management.md` 规范，详细规定了绘图代码独立性、命名规范、模块导出接口、以及运行时依赖等约束，指导后续 AI 助手的协同绘图。


## [v4.1.0] - 2026-05-25
### ⚙️ DevOps & CI/CD 稳定性加固
* **修复了因 Git 忽略文件缺失导致 CI 报错的 Bug**：由于 35MB 的大文件 `data/gis/Building_Footprints.geojson` 被 `.gitignore` 排除，这导致在 GitHub Actions 干净环境中运行 `tools/data_quality_check.py` 会因为检测到该文件缺失而判定为 critical 的 `MISSING` 并返回退出码 1，阻断了 `smoke` 和 `test` job。
* **白名单降级处理**：在 `tools/data_quality_check.py` 中引入 `GIT_IGNORED_FILES` 白名单，将缺失的 Git 忽略大文件自动判定为 `WARNING` 状态并设评级为 `N/A`，不会触发非 0 退出码，保障 CI 无障碍通畅跑通。
* **审计并同步更新文档**：更新了 `BUG_REPORT.md` 与 `PROJECT_INSPECTION_REPORT.md` 相关的体检结论与修复记录。

---

## [v4.0.0] - 2026-05-17
### 🛠️ 工作流与 AI 技能 (Skills) 增强
* **自动更新日志技能**：在本项目根目录 `.gemini/skills/update_changelog.md` 中新增了一项定制技能，强制约束 AI 以后每次在本项目中执行任务时，必须自动在 `CHANGELOG.md` 中记录更新。
* **全局 Superpower 技能引擎**：从 GitHub (`anthonylee991/gemini-superpowers-antigravity`) 下载并部署了完整的 Superpower 开源工作流引擎作为 Gemini 的全局级 Skill，为后续进行复杂的计划制定、并行开发和测试驱动开发提供了坚实的理论和流程保障。

### 🚀 核心架构升级 (Stage 13 & 14 重构)
* **移除了重量级的外部渲染框架**：彻底弃用了在本地运行不稳定、极易崩溃的 Stable Diffusion + ControlNet 渲染管线，以及依赖 Node.js 环境的 HyperFrames 视频框架。
* **人机协同制图工作流 (Stage 13)**：
  * **Python 高精度直出底图**：在 `pages/13_成果表达.py` 中，支持直接调用 `GeoPandas` 与 `Matplotlib` 对 `data/gis` 中的路网、建筑和用地色块进行矢量级精度出图，百分百保障空间逻辑。
  * **Web LLM 重绘规范**：提供针对网页端 Gemini/ChatGPT 的“防篡改提示词”，实现低技术门槛的高保真美术渲染。
  * **自动红头图框引擎**：新增 `src/engines/frame_generator.py` (基于 PIL)，可将用户传回的美化图自动嵌套标准的工程 A3 图框（含图纸编号、中英标题、动态 AI 图面摘要和自动比例尺）。
* **智能分镜与导演脚本系统 (Stage 14)**：
  * 弃用了机械的自动录屏逻辑，转而通过 `stage_data_bus` 动态抓取前序阶段落定的真实评估指标（如 MPI 排行、设计愿景、交通策略等）。
  * 在 `pages/14_视频生成.py` 中自动拼接生成一份精确到分秒的《全流程录屏导演分镜脚本》Markdown 文档，为用户手工录制高质量解说视频提供保姆级指引。
* **精简了项目体积与复杂度**：
  * 移除了 `tools/video_generator/composer` 目录及冗余的 npm 依赖。
  * 移除了项目中大量遗留的 SD 调用脚本。

### 🎨 UI/UX 与视觉体验优化
* **主页底部总结卡片图标防崩溃重构**：将主页底部的三个关键总结卡片（空间评价、视觉推演、协同测算）中的 `<img>` 图标彻底重构为**高兼容性的行内 Lucide SVG 矢量图标**，采用 `currentColor` 动态继承系统色彩，彻底解决了由于本地/云端静态资源路径不一致导致图标破损、显示不正常的问题。
* **研究范围地图 Banner 裁剪与容器贴合优化**：
  * **底图黑边裁剪**：识别到 `research_scope_2d.png` 底图左右两侧存在共计约 `588` 像素宽的纯色空白背景，使用 Python Pillow 图像库对其进行了**高精度自动化无损裁剪**（尺寸从 `2500x2500` 优化为 `1932x2500`），从而最大化地释放了地图与图例的横向像素，内容更加清晰易读。
  * **容器宽高比与贴合重构**：将 CSS 布局属性从 `flex: 1.8` 升级为 **`aspect-ratio: 1932 / 2500`** 完美宽高比约束，使 3D 悬浮图框与内部裁剪后的底图天衣无缝地贴合。
  * **定位与 3D 透视偏移与靠右矫正**：针对 3D 旋转（`rotateY(-25deg)`）导致的视觉左移，引入了 **`transform-origin: right center`** 并缩减了 Banner 的右侧内边距（`padding-right: 25px`），配合 `margin-right: -5px` 完美消除了右侧视觉缝隙，使图框能够以像素级精度钉死在 Banner 的最右侧，完全对齐系统的网格布局线。
  * **左侧自适应拉伸与指标横向铺开**：在保持图框尺寸（高度 `560px`，宽度自适应）完全不变的前提下，让左侧 `.page-banner-content` 面板自适应横向拉伸占据所有空余空间。同时，将指标网格重构为**智能响应式单行横排布局（`repeat(auto-fit, minmax(220px, 1fr))`）**，4项核心指标由 2x2 堆叠优化为单行水平铺开，完美填补了页面中段的视觉空白，整体排版舒展、疏密有致，极具现代高级大屏仪表面板的视觉张力。

### 🔒 安全与仓库管理优化
* **全面硬化敏感信息泄露防线**：
  * 扫描并验证了本地环境，确保所有实际运行的百度 AK 和 DeepSeek API 密钥均安全存放在外部忽略文件 `.env` 中，代码没有任何硬编码的敏感信息。
  * **Git 历史脏数据擦除**：检测到过往开发 Commit 中曾不慎硬编码暴露过百度地图 AK 与 DeepSeek 接口密钥。使用专业的 `git-filter-repo` 工具在项目所有的 **107 个历史 Commit** 中全面覆写检索，对上述两组敏感字符串进行了无损替换（用 `YOUR_BAIDU_MAP_AK` 与 `YOUR_DEEPSEEK_API_KEY` 完美覆盖），并强制推送到 GitHub 覆盖，彻底杜绝了因历史 Commit 被追溯而导致的安全泄露风险。
  * **Claude Code 本地配置安全隔离**：将 Claude Code 本地授权配置文件所在的 `.claude/` 文件夹添加至 `.gitignore` 进行安全忽略；并使用 `git-filter-repo --path .claude/ --invert-paths` 命令，**将历史 Commit 记录中已提交的 `.claude/settings.local.json` 彻底抹去**，同时已在云端 GitHub 仓库完成覆盖，百分之百保证其不在公共网络上可见。
  * 升级 `.gitignore` 配置，新增了 `.gemini/`（AI 代理缓存）、`scratch/`（临时便签空间）和 `output/`（高保真渲染图等大文件）的忽略规则，防止由于误操作而将冗余数据和缓存上传至 GitHub。

### 📚 文档体系更新
* 刷新了 `WORKFLOW.md`、`README.md` 和 `README_EN.md`，将管线描述与新版的轻量化“人机协同”理念完全对齐。
* 更新了 `GLOSSARY.md` 核心术语库，移除了 ControlNet 等概念，增加了 Python 矢量直出、智能分镜等新词汇。

---

## [v3.0.0] - 2026-05-10
### 🚀 数据与制图管线 (GIS-to-AIGC)
* **矢量裁切引擎**：`scripts/clip_city_data.py` 实现对城市级大规模路网 (4.1GB) 和轨交数据的秒级精准裁切，并将研究范围限定于 150 公顷。
* **自动光栅化服务**：将裁切后的 GeoJSON 数据自动转化为 ControlNet 引导图 (Road/Landuse/Satellite Base)。
* **国标用地色值**：`src/engines/spatial_engine.py` 支持 10 类国标用地分类颜色的标准映射。
* **数据目录结构调整**：确立了 `data/gis/` 与 `data/csv/` 的核心架构，由 `src/config/paths.py` 全局接管路径注册。

---

## [v2.0.0] - 2026-04-20
### 🧠 AI 推演引擎升级
* 引入了基于 `stage_data_bus.py` 的跨阶段数据总线机制，彻底解决多页面的状态隔离问题。
* 构建了 `QualityAssessor` 双重评估体系，采用 Gemma 对画面完整性把关，使用 DeepSeek API 评估专业合理性。
* 建立了基于 AHP（层次分析法）的 MPI（微更新潜力指数）评估模型，驱动核心地块的靶向识别。

---

## [v1.0.0] - 2026-03-01
### 🎉 初始发布
* 发布 `ultimateDESIGN` 城乡规划多模态 AI 平台 1.0 版。
* 初步完成 15 个阶段 (Stage 00-14) 的 Streamlit UI 框架搭建。
* 实现了基础的 Deck.GL 3D 建筑底座交互展示能力。
